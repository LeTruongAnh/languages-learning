# Deploy artifacts: cố ý commit tts.tgz + vocab.sql.gz để clone về VPS cho tiện
# (repo PHẢI để Private — dump chứa dữ liệu + hash mật khẩu)
*.sql
# ...nhưng script migration trong repo thì PHẢI commit:
!backend/scripts/*.sql

# Python
backend/.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
backend/.env
backend/uploads/
backend/web-dist/
backend/scripts/uvicorn_seed.*

# Flutter
mobile/.dart_tool/
mobile/build/
mobile/.flutter-plugins*
# android/ ĐƯỢC commit (chứa config desugaring, manifest đã sửa)
mobile/android/local.properties
mobile/android/.gradle/
mobile/ios/
mobile/web/
mobile/windows/
mobile/linux/
mobile/macos/
